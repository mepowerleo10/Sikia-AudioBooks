<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="OpaqueButtonStyle" parent="android:Widget.Holo.Button.Borderless">
        <item name="android:textAllCaps">false</item>
        <item name="android:textAlignment">textStart</item>
        <item name="android:textSize">@dimen/normal_font</item>
        <item name="backgroundTint">@color/textRed</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textColor">@color/textLightDark</item>
    </style>

    <style name="AccountButtons" parent="OpaqueButtonStyle">
        <item name="android:drawableRight">@drawable/ic_baseline_arrow_forward_ios_24</item>
    </style>

    <style name="Separator">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/textLightDark</item>
        <!--        <item name="android:layout_marginHorizontal">22dp</item>-->
    </style>
</resources>